name: qemu-minimal - Report for vm-basic

# The report lanes all share one shape: push to main, a daily cron, and manual.
# Push, because a report describes the merged tree and there is nothing to
# publish from a PR head. All four crons fire together at 01:00 MST (08:00 UTC,
# MST being a fixed -07:00 with no DST) rather than staggered across the small
# hours. This one is unfiltered -- it is the post-merge canary, and at ~3m it is
# cheap enough to run on every merge.
on:
  push:
    branches:
      - main
  schedule:
    - cron: '0 8 * * *'
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: vm-report
  cancel-in-progress: true

jobs:
  generate-report:
    name: Report for vm-basic
    runs-on: ubuntu-latest
    container:
      image: docker.io/sbates130272/batesste-ci-images-ubuntu-qemu-libvfio-user:20260919.g359579e-qemu11.1.1-vfu.8039244
      # Supplies qemu-system-x86_64, qemu-img and cloud-localds.
      options: --device /dev/kvm
    steps:
    - name: Check out code
      uses: actions/checkout@v4.3.1

    # No udev rule here: this job gets /dev/kvm from "options: --device /dev/kvm"
    # and runs as root. The ioctl is the real test -- a bind-mounted node can
    # exist and still be non-functional, and QEMU would silently fall back to TCG.
    - name: Verify KVM is available
      run: |
        test -c /dev/kvm || { echo "::error::/dev/kvm missing - guest would run under TCG"; exit 1; }
        test -r /dev/kvm && test -w /dev/kvm || { echo "::error::/dev/kvm not read-write"; exit 1; }
        python3 -c "import os, fcntl; fcntl.ioctl(os.open('/dev/kvm', os.O_RDWR), 0xAE00, 0)" \
          || { echo "::error::/dev/kvm present but KVM_GET_API_VERSION failed"; exit 1; }
        echo "KVM available: $(ls -l /dev/kvm)"

    # Everything else -- qemu-system-x86_64, qemu-img, cloud-localds, ansible
    # and openssh-client -- already ships in the CI image. Only pip is missing.
    - name: Install missing packages
      run: |
        apt-get update -qq
        apt-get install -y -qq python3-pip

    # Absolute, not "~/.cache/pip": actions/cache expands ~ on the runner and
    # pip expands it in this container, so the restore never reached pip.
    # See .github/actions/setup-vm-job/action.yml.
    - name: Use a fixed pip cache directory
      run: echo "PIP_CACHE_DIR=/var/cache/pip-ci" >> "$GITHUB_ENV"

    - name: Cache pip
      uses: actions/cache@v4
      with:
        path: /var/cache/pip-ci
        key: pip-qemu-tool-v2-${{ hashFiles('qemu/pyproject.toml') }}

    - name: Install qemu-tool
      run: pip install --break-system-packages -e ./qemu

    - name: Generate SSH key-pair
      run: |
        mkdir -p "$HOME/.ssh" && ssh-keygen -t rsa -q -f "$HOME/.ssh/id_rsa" -N ""
        # ssh(1) expands ~ from the passwd entry, not $HOME. A container job
        # runs as root with HOME=/github/home, so the default identity would be
        # looked for at /root/.ssh/id_rsa, not found, and never offered.
        [ "$HOME" = /root ] || { rm -rf /root/.ssh && ln -s "$HOME/.ssh" /root/.ssh; }

    - name: Cache cloud image
      uses: actions/cache@v4
      with:
        path: images/*-server-cloudimg-*.img
        key: cloud-img-amd64-noble-vm-report

    - name: Ensure images directory exists
      run: mkdir -p images

    # Not --packages none, which every other gen-vm call site here uses: the
    # report collects "nvme list" and "lspci", and neither tool is in the
    # stock cloud image. This lane reports on what gen-vm gives you by
    # default, so it uses the default manifest rather than a report-specific
    # one -- it is longer, so watch the SSH wait below if cloud-init slows.
    - name: Generate VM
      run: |
        qemu-tool gen-vm \
          --vm-name vm-report \
          --images images \
          --release noble \
          --username ubuntu \
          --password password \
          --packages qemu/packages.d/packages-default \
          --size 8

    - name: Boot VM
      run: |
        qemu-tool run-vm \
          --vm-name vm-report \
          --images images \
          --nvme 1 \
          > run-vm-output.log 2>&1 &

    - name: Wait for VM SSH (180s timeout)
      run: |
        elapsed=0
        while [ $elapsed -lt 180 ]; do
          if ssh -o ConnectTimeout=1 \
                 -o NoHostAuthenticationForLocalhost=yes \
                 -o StrictHostKeyChecking=no \
                 -p 2222 ubuntu@localhost true 2>/dev/null; then
            echo "VM ready after ${elapsed}s"
            exit 0
          fi
          sleep 2; elapsed=$((elapsed + 2))
        done
        echo "VM failed to boot in time"
        cat run-vm-output.log || true
        exit 1

    # Into site/1vm, not site: the checkout already populated site/ with the
    # repository-owned pages, and uploading that whole tree published a nested
    # copy of the site under 1vm/ on gh-pages. Same shape the rocjitsu and
    # hipfile-fio lanes already use.
    - name: Generate report
      run: bash scripts/generate-vm-report.sh site/1vm 2222 ubuntu

    - name: Show run-vm log on failure
      if: failure()
      run: cat run-vm-output.log

    # Upload only. publish-pages.yml is the sole writer of the site; this
    # lane used to call deploy-pages itself, and a Pages deploy replaces the
    # whole site, so it and vm-report-ernic erased each other's report.
    # No Jekyll build here either: the branch source builds the markdown.
    - name: Upload the report
      uses: actions/upload-artifact@v4
      with:
        name: vm-report-1vm
        path: site/1vm/
        if-no-files-found: error
